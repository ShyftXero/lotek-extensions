# How this extension MOUNTS into a host (lotek). These are the values lotek's
# scripts/stage-extension.sh currently GENERATES into its own [extension] table (passed as CLI args at
# vendor time). Carried here so an INSTALLED package (pinned git dep) is self-describing: the host can
# read its mount metadata straight from the shipped manifest instead of the operator re-supplying
# entrypoint/url_prefix/seed. Named [mount], NOT [extension], on purpose — the staging script still
# emits its own [extension] table before appending this file, and TOML forbids a duplicate table, so
# an [extension] header here would make tomllib raise and silently un-discover the whole extension.
[mount]
name = "vector"
entrypoint = "vector"          # importable module exposing register(...)
url_prefix = "/vector"
seed = "vector.seed:seed_defaults"   # module:callable run once after register() (idempotent)

# What Vector exposes to a host (e.g. lotek). Consumed at vendor time by the host's
# scripts/stage-extension.sh and appended verbatim to the generated extensions/vector/extension.toml,
# so the host renders exactly the sidebar entries declared here. `path` is relative to the extension's
# url_prefix; `icon` is an optional glyph. Order here is the sidebar order.
#
# ONE sidebar entry only. The `/` (list) and `/diagrams` routes render the SAME view
# (blueprint.dashboard carries both decorators), so a second "Attack Paths" nav link was a duplicate
# of this one — it pointed operators at the identical page. `/diagrams` remains a live route (for
# existing links/bookmarks); it just no longer gets its own sidebar entry.
[[nav]]
label = "Vector"
path = "/"
icon = "◭"

# How this extension plugs into generic HOST surfaces. A NEW top-level table on purpose: the staging
# script already emits its own [extension] table before appending this file, and TOML forbids a
# duplicate table — an [extension] header here would make tomllib raise and silently un-discover the
# whole extension.
[host]
# Path (relative to url_prefix) of this extension's PAT/Bearer MACHINE api. The host treats it as a
# machine surface: skipped by the cookie/session dashboard gate and CSRF-exempt, exactly like
# lotek's own /api/v1. MUST be a proper sub-path of url_prefix, and MUST NOT overlap the
# cookie-authed browser JSON api at <url_prefix>/api.
machine_prefix = "/machine"
# Table in the shared DB whose rows soft-reference a host Client (client_id). The host's client-delete
# guard counts these generically so deleting a client can't orphan them (lotek#585, data-lifecycle).
client_ref_table = "vector_diagrams"
client_ref_label = "diagram"
# Operator doc for the host Docs page. Optional — omitted, the host looks for docs/<NAME>.md.
docs = "docs/VECTOR.md"
docs_title = "Vector (attack paths)"

# Tables this extension OWNS (see the equivalent block in Scribble): the host may carry them through a
# database migration, attribute them for drop/truncate, and never touch a table nobody claims.
[db]
base = "vector.models:Base"   # the module whose IMPORT registers the tables (db.py alone yields an empty Base)
table_prefix = "vector_"

# ADMIN-scope, per-install settings the HOST renders, gates and stores (lotek#485). The host puts a ⚙ cog
# on Vector's row at /settings/extensions, shows this form behind its own ADMIN gate, writes the value to
# its `settings` table under `ext:vector:<key>`, and audits every change. Vector reads it back through
# `extras["extension_setting"]` (see vector/deps.py::host_setting) and never stores or gates it itself.
#
# USER-scope preferences are deliberately NOT here (lotek-extensions#111): they cross no privilege
# boundary, so they live in Vector's own `vector_user_prefs` table behind Vector's own ⚙ cog at
# /vector/settings. A knob belongs here only if changing it changes the install for EVERYONE.
[[settings]]
key = "deliverable_footer"
label = "Deliverable footer"
type = "str"
default = ""
help = """Optional line stamped into the footer of every exported attack-path HTML — e.g. \
"CONFIDENTIAL — prepared by Contoso Security". Per-install: it is the same on every deliverable this \
dashboard produces, which is why it is admin-only. Leave blank for no footer."""
